// ThemeSelector removed — kept a minimal stub to avoid build errors if referenced.
// Use the standalone `/tema` page instead (src/pages/Theme.jsx).
const ThemeSelector = () => null

export default ThemeSelector
